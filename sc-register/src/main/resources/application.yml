server:
  port: 6543
spring:
  application:
    name: sc-register
logging:
  config: classpath:logback.xml
eureka:
  instance:
    hostname: sc-register
  client:
    register-with-eureka: false
    fetch-registry: false
